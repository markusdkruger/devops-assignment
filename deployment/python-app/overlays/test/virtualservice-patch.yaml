apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: hello-world-virtualservice
spec:
  hosts:
    - "test.devopsassignment.com"
  http:
    - route:
        - destination:
            host: test-helloworld-svc